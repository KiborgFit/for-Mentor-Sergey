const moduleWindow = document.getElementById('block');

const buttonEnter = document.getElementById('btn');

const buttonClose = document.getElementById('button');

buttonEnter.addEventListener('click', () => {moduleWindow.style.visibility = "visible"});

btnEnter.addEventListener('click', () => {moduleWindow.style.animationPlayState = "string"});

